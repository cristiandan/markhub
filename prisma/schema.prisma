// Markhub Prisma Schema
// Database: PostgreSQL on Render

generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}

// Models will be added in P1-08 (Database schema design)
// Initial models planned:
// - User (GitHub OAuth)
// - Repository (markdown file collections)
// - File (individual markdown files)
// - Star (user favorites)
